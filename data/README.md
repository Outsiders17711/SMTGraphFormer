Place the raw stop level pickle file here.

Expected filename: `atbData-May2024-stopLevel-[fPM.eST.eLU.eDW].pkl`

The data integration pipeline in `notebooks/dataIntegration.ipynb` writes the shared artefacts used by the SMT training script and benchmark notebooks into this directory.
